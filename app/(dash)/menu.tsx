import React from "react";
import { View } from "react-native";

// This is a placeholder screen for the menu tab.
// The actual menu functionality is handled by the HeaderMenu component in the tab bar.
export default function MenuScreen() {
  return <View />;
}
